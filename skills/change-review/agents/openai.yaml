interface:
  display_name: "변경 검토"
  short_description: "change-review · 작업 조건에 맞는 범위만 적용"
  default_prompt: "$change-review를 사용해 이 변경의 정확성 및 회귀 위험을 검토해 주세요."

# 기존 Astra 이후의 얇은 지침을 유지한다. 전체 절차는 명시 요청 때만 사용한다.
policy:
  allow_implicit_invocation: true
