interface:
  display_name: "웹 성능 진단"
  short_description: "performance · 작업 조건에 맞는 범위만 적용"
  default_prompt: "$performance 현재 작업의 관련 범위만 검토하고 실제 실행과 미실행을 구분해줘."
policy:
  allow_implicit_invocation: true
