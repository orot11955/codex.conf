interface:
  display_name: "변경 검증"
  short_description: "change-verification · 작업 조건에 맞는 범위만 적용"
  default_prompt: "$change-verification을 사용해 이 변경을 재현하고 완료 조건을 검증해 주세요."

# 기존 Astra 이후의 얇은 지침을 유지한다. 전체 절차는 명시 요청 때만 사용한다.
policy:
  allow_implicit_invocation: true
